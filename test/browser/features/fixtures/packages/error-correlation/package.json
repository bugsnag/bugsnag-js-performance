{
    "name": "error-correlation",
    "private": true,
    "scripts": {
        "build": "rollup --config ../rollup.config.mjs",
        "clean": "rm -rf dist"
    },
    "peerDependencies": {
        "@bugsnag/browser": ">= 8.0.0-alpha.12"
    }
}
